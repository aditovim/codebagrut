# מה תוקן בסבב הזה — ותיקנתי אותו ישירות בקוד, לא רק כהנחיה לבולט

## 1. שני ה-Edge Functions היו עדיין מזויפים לחלוטין

**מה מצאתי:** גם `review-code` וגם `ai-tutor` לא קראו ל-Anthropic בשום מקום. `review-code` חישב ציון
לפי בדיקת מילות מפתח (for/if/class/return...) עם ניקוד קבוע. `ai-tutor` בחר תשובה ממערך של 7
משפטים מוכנים מראש, לפי מילת מפתח בשאלה או אקראית.

**מה תיקנתי:** שכתבתי את שני הקבצים מחדש (`supabase/functions/review-code/index.ts` ו-
`supabase/functions/ai-tutor/index.ts`) כך שהם קוראים ל-Anthropic באמת, ושומרים בדיוק על אותה
לוגיקת אימות משתמש (JWT) והגבלת קצב (rate limiting) שכבר הייתה קיימת - שינוי ממוקד, לא שיפוץ כולל.

אם `ANTHROPIC_API_KEY` לא מוגדר כ-secret בפרויקט Supabase, הפונקציה תחזיר שגיאה ברורה
במקום ליפול חזרה לאיזושהי סימולציה - כדי שלעולם לא תדע "בטעות" שהיא עובדת כשהיא לא.

## 2. באג נוסף שגיליתי: התחברות שגויה לכל קריאת AI

**מה מצאתי:** הבקשות מהדפדפן לשתי הפונקציות שלחו את `VITE_SUPABASE_ANON_KEY` (מפתח ציבורי כללי)
בתור אימות המשתמש. אבל הפונקציות בצד השרת מזהות *איזה משתמש* קורא להן לפי הטוקן הזה
(כדי לאכוף הגבלת קצב לכל תלמיד בנפרד) - ומפתח אנונימי לא מייצג משתמש מחובר ספציפי.
בפועל זה כנראה גרם לכל הקריאות להיכשל עם שגיאת 401 (לא מורשה), עוד לפני שהגענו בכלל לשאלת
"האם ה-AI אמיתי או מזויף".

**מה תיקנתי:** ב-`src/lib/ai.ts`, במקום לשלוח את המפתח הכללי, הקוד עכשיו שולף את הטוקן האמיתי
של המשתמש המחובר (`supabase.auth.getSession()`) ושולח אותו. תיקנתי את זה גם עבור בדיקת קוד
וגם עבור צ'אט המנטור.

## 3. תיקון נלווה: הסתרת שגיאות בצ'אט המנטור

**מה מצאתי:** ב-`Practice.tsx`, אם קריאת ה-AI נכשלה מכל סיבה (רשת, שרת, וכו') - הקוד הציג לתלמיד
תשובה גנרית ("נסה לחשוב שלב אחר שלב") שנראית כמו תשובת AI לגיטימית, במקום להראות שהייתה שגיאה.
זה בדיוק סוג הדבר שגורם לבעיה להסתתר בלי שאף אחד ישים לב.

**מה תיקנתי:** עכשיו שגיאה אמיתית מוצגת כשגיאה (או הודעת "הגעת למגבלת השימוש" אם זו הסיבה),
לא מוסווית כתשובת מנטור תקינה.

## מה עדיין נדרש ממך כדי שזה יעבוד בפועל

זהה למה שכתבתי ב-FIXES.md הקודם:

1. ודא שיש לך מפתח מ-console.anthropic.com
2. הגדר אותו כ-secret: `supabase secrets set ANTHROPIC_API_KEY=המפתח-שלך`
3. פרוס את שתי הפונקציות המעודכנות:
   ```
   supabase functions deploy review-code
   supabase functions deploy ai-tutor
   ```
4. בדקו בפועל: הירשמו כתלמיד, פתרו תרגיל, לחצו "בדוק קוד" - הפעם התשובה תיקח כמה שניות
   (לא מיידית כמו קודם, כי זו קריאה אמיתית ל-AI) ותהיה שונה בכל פעם, לא ניקוד קבוע לפי מילות מפתח.

## לגבי הנתונים המזויפים (מהסבב הקודם)

לא בדקתי מחדש בסבב הזה אם 6 התלמידים המזויפים חזרו למסד הנתונים - זה תלוי אם הרצתם את המיגרציה
שנתתי קודם (`20260825110000_remove_fake_demo_data_restore_fk.sql`). כדאי לוודא שזה עדיין בתוקף
בפרויקט ה-Supabase האמיתי שלכם (לא רק בקובץ המקומי) - זה נבדק ב-SQL Editor של Supabase, לא בקוד.
