# מעבר ל-Gemini (חינמי) במקום Anthropic (בתשלום)

## מה השתנה

עדכנתי את שני קבצי ה-Edge Functions:
- `supabase/functions/review-code/index.ts`
- `supabase/functions/ai-tutor/index.ts`

הם עכשיו קוראים ל-**Google Gemini API** (מודל `gemini-2.5-flash`) במקום ל-Anthropic. שני הקבצים
שומרים בדיוק על אותה לוגיקת אימות משתמש והגבלת קצב (rate limiting) שכבר הייתה קיימת -
רק מנוע ה-AI עצמו הוחלף.

## חשוב להבין: המפתח לא הולך לקובץ .env הרגיל

**אל תשימו את המפתח ב-.env של הפרויקט (זה קובץ שנחשף בדפדפן)** — כפי שכתוב בהודעה שקיבלת,
זה מבלבל קצת: המפתח צריך לשבת רק בצד השרת (ב-Supabase), לא בקוד הצד-לקוח. ה-Edge Functions
קוראות אותו דרך `Deno.env.get('GEMINI_API_KEY')` — משתנה שמוגדר כ-"secret" בפרויקט ה-Supabase
שלכם, לא בקובץ .env של ה-React app.

## שלבים להפעלה (בחינם לגמרי)

1. **קבלת מפתח Gemini חינמי:**
   - היכנסו ל-https://aistudio.google.com/
   - התחברו עם חשבון Google
   - לחצו "Get API key" / "צור מפתח API"
   - **אל תפעילו billing (חיוב)** על הפרויקט הזה ב-Google Cloud — ברגע שמפעילים חיוב, הטייר
     החינמי נעלם באותו פרויקט. השאירו אותו ללא חיוב כדי שהוא יישאר חינמי.

2. **הגדרת המפתח כ-secret ב-Supabase** (כמו קודם, רק שם המשתנה שונה):
   ```
   supabase secrets set GEMINI_API_KEY=המפתח-שלכם-מ-AI-Studio
   ```

3. **פריסת שתי הפונקציות המעודכנות:**
   ```
   supabase functions deploy review-code
   supabase functions deploy ai-tutor
   ```

## מגבלות הטייר החינמי שכדאי להכיר (לפיילוט של כיתה אחת, זה יותר ממספיק)

- כ-1,500 בקשות ביום למודל `gemini-2.5-flash`
- כ-15 בקשות בדקה
- אין תפוגה, אין צורך בכרטיס אשראי

בשילוב עם ה-rate limit הפנימי שכבר קיים אצלכם (20 קריאות לשעה לכל תלמיד), זה אמור להספיק
בנוחות לפיילוט של כיתה שלמה בלי שתתקרבו בכלל למגבלה היומית של גוגל.

**הערה:** אם מגבלת ה-15 בקשות/דקה תתברר כצפופה מדי (למשל אם כמה תלמידים לוחצים "בדוק קוד"
בו-זמנית ממש), אפשר להחליף בקובץ את `GEMINI_MODEL` מ-`"gemini-2.5-flash"` ל-
`"gemini-2.5-flash-lite"` — מהיר יותר, מגבלת בקשות-לדקה גבוהה יותר, באיכות מעט נמוכה יותר.

## מה נשאר זהה

- כל שאר האתר (הרשמה, מסד נתונים, מפת מיומנויות, מבחן מדמה, שיעורי בית, קהילת מורים) לא נגע בו כלל.
- ה-Authorization header שנשלח מהדפדפן עדיין הטוקן האמיתי של המשתמש המחובר (התיקון מהסבב הקודם) -
  זה לא קשור לספק ה-AI ונשאר כמו שהיה.
